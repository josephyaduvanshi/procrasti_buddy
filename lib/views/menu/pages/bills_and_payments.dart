import 'package:flutter/material.dart';

class BillsAndPaymentPage extends StatelessWidget {
  const BillsAndPaymentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
